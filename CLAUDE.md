# Compatibility Note

This repo now uses `AGENTS.md` as the primary instruction entry point for Codex and coding-agent sessions.

Claude-based tools should also read `AGENTS.md` and the supporting files under `docs/agent-system/` instead of maintaining a separate instruction system here.
